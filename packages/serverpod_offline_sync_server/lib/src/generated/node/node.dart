/* AUTOMATICALLY GENERATED CODE DO NOT MODIFY */
/*   To generate run: "serverpod generate"    */

// ignore_for_file: implementation_imports
// ignore_for_file: library_private_types_in_public_api
// ignore_for_file: non_constant_identifier_names
// ignore_for_file: public_member_api_docs
// ignore_for_file: type_literal_in_constant_pattern
// ignore_for_file: use_super_parameters
// ignore_for_file: invalid_use_of_internal_member

// ignore_for_file: no_leading_underscores_for_library_prefixes
import 'package:serverpod/serverpod.dart' as _i1;
import 'package:serverpod_offline_sync/serverpod_offline_sync.dart' as _i2;

/// CRDT nodes table.
///
/// This table stores stable CRDT replica identities that have been seen.
abstract class CrdtNode
    implements _i1.TableRow<int?>, _i1.ProtocolSerialization {
  CrdtNode._({
    this.id,
    _i1.UuidValue? uuidNodeId,
    this.lastHlc,
  }) : uuidNodeId = uuidNodeId ?? const _i1.Uuid().v7obj();

  factory CrdtNode({
    int? id,
    _i1.UuidValue? uuidNodeId,
    _i2.Hlc? lastHlc,
  }) = _CrdtNodeImpl;

  factory CrdtNode.fromJson(Map<String, dynamic> jsonSerialization) {
    return CrdtNode(
      id: jsonSerialization['id'] as int?,
      uuidNodeId: jsonSerialization['uuidNodeId'] == null
          ? null
          : _i1.UuidValueJsonExtension.fromJson(
              jsonSerialization['uuidNodeId'],
            ),
      lastHlc: jsonSerialization['lastHlc'] == null
          ? null
          : _i2.Hlc.fromJson(jsonSerialization['lastHlc']),
    );
  }

  static final t = CrdtNodeTable();

  static const db = CrdtNodeRepository._();

  @override
  int? id;

  /// Stable unique identifier for the replica/database install.
  _i1.UuidValue uuidNodeId;

  /// Last HLC generated by the local replica when this row is the current node.
  _i2.Hlc? lastHlc;

  @override
  _i1.Table<int?> get table => t;

  /// Returns a shallow copy of this [CrdtNode]
  /// with some or all fields replaced by the given arguments.
  @_i1.useResult
  CrdtNode copyWith({
    int? id,
    _i1.UuidValue? uuidNodeId,
    _i2.Hlc? lastHlc,
  });
  @override
  Map<String, dynamic> toJson() {
    return {
      '__className__': 'serverpod_offline_sync.CrdtNode',
      if (id != null) 'id': id,
      'uuidNodeId': uuidNodeId.toJson(),
      if (lastHlc != null) 'lastHlc': lastHlc?.toJson(),
    };
  }

  @override
  Map<String, dynamic> toJsonForProtocol() {
    return {
      '__className__': 'serverpod_offline_sync.CrdtNode',
      if (id != null) 'id': id,
      'uuidNodeId': uuidNodeId.toJson(),
      if (lastHlc != null)
        'lastHlc':
            // ignore: unnecessary_type_check
            lastHlc is _i1.ProtocolSerialization
            ? (lastHlc as _i1.ProtocolSerialization).toJsonForProtocol()
            :
              // ignore: dead_code
              lastHlc?.toJson(),
    };
  }

  static CrdtNodeInclude include() {
    return CrdtNodeInclude._();
  }

  static CrdtNodeIncludeList includeList({
    _i1.WhereExpressionBuilder<CrdtNodeTable>? where,
    int? limit,
    int? offset,
    _i1.OrderByBuilder<CrdtNodeTable>? orderBy,
    _i1.OrderByListBuilder<CrdtNodeTable>? orderByList,
    CrdtNodeInclude? include,
  }) {
    return CrdtNodeIncludeList._(
      where: where,
      limit: limit,
      offset: offset,
      orderBy: orderBy?.call(CrdtNode.t),
      orderByList: orderByList?.call(CrdtNode.t),
      include: include,
    );
  }

  @override
  String toString() {
    return _i1.SerializationManager.encode(this);
  }
}

class _Undefined {}

class _CrdtNodeImpl extends CrdtNode {
  _CrdtNodeImpl({
    int? id,
    _i1.UuidValue? uuidNodeId,
    _i2.Hlc? lastHlc,
  }) : super._(
         id: id,
         uuidNodeId: uuidNodeId,
         lastHlc: lastHlc,
       );

  /// Returns a shallow copy of this [CrdtNode]
  /// with some or all fields replaced by the given arguments.
  @_i1.useResult
  @override
  CrdtNode copyWith({
    Object? id = _Undefined,
    _i1.UuidValue? uuidNodeId,
    Object? lastHlc = _Undefined,
  }) {
    return CrdtNode(
      id: id is int? ? id : this.id,
      uuidNodeId: uuidNodeId ?? this.uuidNodeId,
      lastHlc: lastHlc is _i2.Hlc? ? lastHlc : this.lastHlc?.copyWith(),
    );
  }
}

class CrdtNodeUpdateTable extends _i1.UpdateTable<CrdtNodeTable> {
  CrdtNodeUpdateTable(super.table);

  _i1.ColumnValue<_i1.UuidValue, _i1.UuidValue> uuidNodeId(
    _i1.UuidValue value,
  ) => _i1.ColumnValue(
    table.uuidNodeId,
    value,
  );

  _i1.ColumnValue<_i2.Hlc, _i2.Hlc> lastHlc(_i2.Hlc? value) => _i1.ColumnValue(
    table.lastHlc,
    value,
  );
}

class CrdtNodeTable extends _i1.Table<int?> {
  CrdtNodeTable({super.tableRelation}) : super(tableName: 'crdt_nodes') {
    updateTable = CrdtNodeUpdateTable(this);
    uuidNodeId = _i1.ColumnUuid(
      'uuidNodeId',
      this,
      hasDefault: true,
    );
    lastHlc = _i1.ColumnStructured<_i2.Hlc>(
      'lastHlc',
      this,
    );
  }

  late final CrdtNodeUpdateTable updateTable;

  /// Stable unique identifier for the replica/database install.
  late final _i1.ColumnUuid uuidNodeId;

  /// Last HLC generated by the local replica when this row is the current node.
  late final _i1.ColumnStructured<_i2.Hlc> lastHlc;

  @override
  List<_i1.Column> get columns => [
    id,
    uuidNodeId,
    lastHlc,
  ];
}

class CrdtNodeInclude extends _i1.IncludeObject {
  CrdtNodeInclude._();

  @override
  Map<String, _i1.Include?> get includes => {};

  @override
  _i1.Table<int?> get table => CrdtNode.t;
}

class CrdtNodeIncludeList extends _i1.IncludeList {
  CrdtNodeIncludeList._({
    _i1.WhereExpressionBuilder<CrdtNodeTable>? where,
    super.limit,
    super.offset,
    super.orderBy,
    super.orderByList,
    super.include,
  }) {
    super.where = where?.call(CrdtNode.t);
  }

  @override
  Map<String, _i1.Include?> get includes => include?.includes ?? {};

  @override
  _i1.Table<int?> get table => CrdtNode.t;
}

class CrdtNodeRepository {
  const CrdtNodeRepository._();

  /// Returns a list of [CrdtNode]s matching the given query parameters.
  ///
  /// Use [where] to specify which items to include in the return value.
  /// If none is specified, all items will be returned.
  ///
  /// To specify the order of the items use [orderBy] or [orderByList]
  /// when sorting by multiple columns.
  ///
  /// The maximum number of items can be set by [limit]. If no limit is set,
  /// all items matching the query will be returned.
  ///
  /// [offset] defines how many items to skip, after which [limit] (or all)
  /// items are read from the database.
  ///
  /// ```dart
  /// var persons = await Persons.db.find(
  ///   session,
  ///   where: (t) => t.lastName.equals('Jones'),
  ///   orderBy: (t) => t.firstName,
  ///   limit: 100,
  /// );
  /// ```
  Future<List<CrdtNode>> find(
    _i1.DatabaseSession session, {
    _i1.WhereExpressionBuilder<CrdtNodeTable>? where,
    int? limit,
    int? offset,
    _i1.OrderByBuilder<CrdtNodeTable>? orderBy,
    _i1.OrderByListBuilder<CrdtNodeTable>? orderByList,
    _i1.Transaction? transaction,
    _i1.LockMode? lockMode,
    _i1.LockBehavior? lockBehavior,
  }) async {
    return session.db.find<CrdtNode>(
      where: where?.call(CrdtNode.t),
      orderBy: orderBy?.call(CrdtNode.t),
      orderByList: orderByList?.call(CrdtNode.t),
      limit: limit,
      offset: offset,
      transaction: transaction,
      lockMode: lockMode,
      lockBehavior: lockBehavior,
    );
  }

  /// Returns the first matching [CrdtNode] matching the given query parameters.
  ///
  /// Use [where] to specify which items to include in the return value.
  /// If none is specified, all items will be returned.
  ///
  /// To specify the order use [orderBy] or [orderByList]
  /// when sorting by multiple columns.
  ///
  /// [offset] defines how many items to skip, after which the next one will be picked.
  ///
  /// ```dart
  /// var youngestPerson = await Persons.db.findFirstRow(
  ///   session,
  ///   where: (t) => t.lastName.equals('Jones'),
  ///   orderBy: (t) => t.age,
  /// );
  /// ```
  Future<CrdtNode?> findFirstRow(
    _i1.DatabaseSession session, {
    _i1.WhereExpressionBuilder<CrdtNodeTable>? where,
    int? offset,
    _i1.OrderByBuilder<CrdtNodeTable>? orderBy,
    _i1.OrderByListBuilder<CrdtNodeTable>? orderByList,
    _i1.Transaction? transaction,
    _i1.LockMode? lockMode,
    _i1.LockBehavior? lockBehavior,
  }) async {
    return session.db.findFirstRow<CrdtNode>(
      where: where?.call(CrdtNode.t),
      orderBy: orderBy?.call(CrdtNode.t),
      orderByList: orderByList?.call(CrdtNode.t),
      offset: offset,
      transaction: transaction,
      lockMode: lockMode,
      lockBehavior: lockBehavior,
    );
  }

  /// Finds a single [CrdtNode] by its [id] or null if no such row exists.
  Future<CrdtNode?> findById(
    _i1.DatabaseSession session,
    int id, {
    _i1.Transaction? transaction,
    _i1.LockMode? lockMode,
    _i1.LockBehavior? lockBehavior,
  }) async {
    return session.db.findById<CrdtNode>(
      id,
      transaction: transaction,
      lockMode: lockMode,
      lockBehavior: lockBehavior,
    );
  }

  /// Inserts all [CrdtNode]s in the list and returns the inserted rows.
  ///
  /// The returned [CrdtNode]s will have their `id` fields set.
  ///
  /// This is an atomic operation, meaning that if one of the rows fails to
  /// insert, none of the rows will be inserted.
  ///
  /// If [ignoreConflicts] is set to `true`, rows that conflict with existing
  /// rows are silently skipped, and only the successfully inserted rows are
  /// returned.
  ///
  /// If [noReturn] is set to `true`, the inserted rows are not read back from
  /// the database and an empty list is returned. This avoids the overhead of
  /// transferring and deserializing the rows when the result is not needed.
  Future<List<CrdtNode>> insert(
    _i1.DatabaseSession session,
    List<CrdtNode> rows, {
    _i1.Transaction? transaction,
    bool ignoreConflicts = false,
    bool noReturn = false,
  }) async {
    return session.db.insert<CrdtNode>(
      rows,
      transaction: transaction,
      ignoreConflicts: ignoreConflicts,
      noReturn: noReturn,
    );
  }

  /// Inserts a single [CrdtNode] and returns the inserted row.
  ///
  /// The returned [CrdtNode] will have its `id` field set.
  Future<CrdtNode> insertRow(
    _i1.DatabaseSession session,
    CrdtNode row, {
    _i1.Transaction? transaction,
  }) async {
    return session.db.insertRow<CrdtNode>(
      row,
      transaction: transaction,
    );
  }

  /// Upserts all [CrdtNode]s in the list and returns the resulting rows.
  ///
  /// If a row conflicts on the given [conflictColumns], the existing row is
  /// updated with the new values. Otherwise, a new row is inserted.
  ///
  /// If [updateColumns] is provided, only those columns will be updated on
  /// conflict. If null, all non-conflict, non-id columns are updated.
  ///
  /// If [updateWhere] is provided, the update only applies to rows matching the
  /// given expression. Conflicting rows that don't match are skipped and not
  /// returned, so the resulting list may be shorter than [rows].
  ///
  /// The returned [CrdtNode]s will have their `id` fields set.
  ///
  /// This is an atomic operation, meaning that if one of the rows fails,
  /// none of the rows will be affected.
  ///
  /// If [noReturn] is set to `true`, the resulting rows are not read back from
  /// the database and an empty list is returned. This avoids the overhead of
  /// transferring and deserializing the rows when the result is not needed.
  Future<List<CrdtNode>> upsert(
    _i1.DatabaseSession session,
    List<CrdtNode> rows, {
    required _i1.ColumnSelections<CrdtNodeTable> conflictColumns,
    _i1.ColumnSelections<CrdtNodeTable>? updateColumns,
    _i1.WhereExpressionBuilder<CrdtNodeTable>? updateWhere,
    _i1.Transaction? transaction,
    bool noReturn = false,
  }) async {
    return session.db.upsert<CrdtNode>(
      rows,
      conflictColumns: conflictColumns(CrdtNode.t),
      updateColumns: updateColumns?.call(CrdtNode.t),
      updateWhere: updateWhere?.call(CrdtNode.t),
      transaction: transaction,
      noReturn: noReturn,
    );
  }

  /// Upserts a single [CrdtNode] and returns the resulting row.
  ///
  /// If the row conflicts on the given [conflictColumns], the existing row is
  /// updated. Otherwise, a new row is inserted.
  ///
  /// If [updateColumns] is provided, only those columns will be updated on
  /// conflict. If null, all non-conflict, non-id columns are updated.
  ///
  /// If [updateWhere] is provided, the update only applies when the existing
  /// row matches the expression. Returns `null` if no row was affected — for
  /// example when [updateWhere] does not match the conflicting row.
  ///
  /// The returned [CrdtNode] will have its `id` field set.
  Future<CrdtNode?> upsertRow(
    _i1.DatabaseSession session,
    CrdtNode row, {
    required _i1.ColumnSelections<CrdtNodeTable> conflictColumns,
    _i1.ColumnSelections<CrdtNodeTable>? updateColumns,
    _i1.WhereExpressionBuilder<CrdtNodeTable>? updateWhere,
    _i1.Transaction? transaction,
  }) async {
    return session.db.upsertRow<CrdtNode>(
      row,
      conflictColumns: conflictColumns(CrdtNode.t),
      updateColumns: updateColumns?.call(CrdtNode.t),
      updateWhere: updateWhere?.call(CrdtNode.t),
      transaction: transaction,
    );
  }

  /// Updates all [CrdtNode]s in the list and returns the updated rows. If
  /// [columns] is provided, only those columns will be updated. Defaults to
  /// all columns.
  /// This is an atomic operation, meaning that if one of the rows fails to
  /// update, none of the rows will be updated.
  ///
  /// If [noReturn] is set to `true`, the updated rows are not read back from
  /// the database and an empty list is returned. This avoids the overhead of
  /// transferring and deserializing the rows when the result is not needed.
  Future<List<CrdtNode>> update(
    _i1.DatabaseSession session,
    List<CrdtNode> rows, {
    _i1.ColumnSelections<CrdtNodeTable>? columns,
    _i1.Transaction? transaction,
    bool noReturn = false,
  }) async {
    return session.db.update<CrdtNode>(
      rows,
      columns: columns?.call(CrdtNode.t),
      transaction: transaction,
      noReturn: noReturn,
    );
  }

  /// Updates a single [CrdtNode]. The row needs to have its id set.
  /// Optionally, a list of [columns] can be provided to only update those
  /// columns. Defaults to all columns.
  Future<CrdtNode> updateRow(
    _i1.DatabaseSession session,
    CrdtNode row, {
    _i1.ColumnSelections<CrdtNodeTable>? columns,
    _i1.Transaction? transaction,
  }) async {
    return session.db.updateRow<CrdtNode>(
      row,
      columns: columns?.call(CrdtNode.t),
      transaction: transaction,
    );
  }

  /// Updates a single [CrdtNode] by its [id] with the specified [columnValues].
  /// Returns the updated row or null if no row with the given id exists.
  Future<CrdtNode?> updateById(
    _i1.DatabaseSession session,
    int id, {
    required _i1.ColumnValueListBuilder<CrdtNodeUpdateTable> columnValues,
    _i1.Transaction? transaction,
  }) async {
    return session.db.updateById<CrdtNode>(
      id,
      columnValues: columnValues(CrdtNode.t.updateTable),
      transaction: transaction,
    );
  }

  /// Updates all [CrdtNode]s matching the [where] expression with the specified [columnValues].
  /// Returns the list of updated rows.
  ///
  /// If [noReturn] is set to `true`, the updated rows are not read back from
  /// the database and an empty list is returned. This avoids the overhead of
  /// transferring and deserializing the rows when the result is not needed.
  Future<List<CrdtNode>> updateWhere(
    _i1.DatabaseSession session, {
    required _i1.ColumnValueListBuilder<CrdtNodeUpdateTable> columnValues,
    required _i1.WhereExpressionBuilder<CrdtNodeTable> where,
    int? limit,
    int? offset,
    _i1.OrderByBuilder<CrdtNodeTable>? orderBy,
    _i1.OrderByListBuilder<CrdtNodeTable>? orderByList,
    _i1.Transaction? transaction,
    bool noReturn = false,
  }) async {
    return session.db.updateWhere<CrdtNode>(
      columnValues: columnValues(CrdtNode.t.updateTable),
      where: where(CrdtNode.t),
      limit: limit,
      offset: offset,
      orderBy: orderBy?.call(CrdtNode.t),
      orderByList: orderByList?.call(CrdtNode.t),
      transaction: transaction,
      noReturn: noReturn,
    );
  }

  /// Deletes all [CrdtNode]s in the list and returns the deleted rows.
  ///
  /// To specify the order of the returned rows use [orderBy] or [orderByList]
  /// when sorting by multiple columns.
  ///
  /// This is an atomic operation, meaning that if one of the rows fail to
  /// be deleted, none of the rows will be deleted.
  ///
  /// If [noReturn] is set to `true`, the deleted rows are not read back from
  /// the database and an empty list is returned. This avoids the overhead of
  /// transferring and deserializing the rows when the result is not needed.
  Future<List<CrdtNode>> delete(
    _i1.DatabaseSession session,
    List<CrdtNode> rows, {
    _i1.OrderByBuilder<CrdtNodeTable>? orderBy,
    _i1.OrderByListBuilder<CrdtNodeTable>? orderByList,
    _i1.Transaction? transaction,
    bool noReturn = false,
  }) async {
    return session.db.delete<CrdtNode>(
      rows,
      orderBy: orderBy?.call(CrdtNode.t),
      orderByList: orderByList?.call(CrdtNode.t),
      transaction: transaction,
      noReturn: noReturn,
    );
  }

  /// Deletes a single [CrdtNode].
  Future<CrdtNode> deleteRow(
    _i1.DatabaseSession session,
    CrdtNode row, {
    _i1.Transaction? transaction,
  }) async {
    return session.db.deleteRow<CrdtNode>(
      row,
      transaction: transaction,
    );
  }

  /// Deletes all rows matching the [where] expression.
  ///
  /// To specify the order of the returned rows use [orderBy] or [orderByList]
  /// when sorting by multiple columns.
  ///
  /// If [noReturn] is set to `true`, the deleted rows are not read back from
  /// the database and an empty list is returned. This avoids the overhead of
  /// transferring and deserializing the rows when the result is not needed.
  Future<List<CrdtNode>> deleteWhere(
    _i1.DatabaseSession session, {
    required _i1.WhereExpressionBuilder<CrdtNodeTable> where,
    _i1.OrderByBuilder<CrdtNodeTable>? orderBy,
    _i1.OrderByListBuilder<CrdtNodeTable>? orderByList,
    _i1.Transaction? transaction,
    bool noReturn = false,
  }) async {
    return session.db.deleteWhere<CrdtNode>(
      where: where(CrdtNode.t),
      orderBy: orderBy?.call(CrdtNode.t),
      orderByList: orderByList?.call(CrdtNode.t),
      transaction: transaction,
      noReturn: noReturn,
    );
  }

  /// Counts the number of rows matching the [where] expression. If omitted,
  /// will return the count of all rows in the table.
  Future<int> count(
    _i1.DatabaseSession session, {
    _i1.WhereExpressionBuilder<CrdtNodeTable>? where,
    int? limit,
    _i1.Transaction? transaction,
  }) async {
    return session.db.count<CrdtNode>(
      where: where?.call(CrdtNode.t),
      limit: limit,
      transaction: transaction,
    );
  }

  /// Acquires row-level locks on [CrdtNode] rows matching the [where] expression.
  Future<void> lockRows(
    _i1.DatabaseSession session, {
    required _i1.WhereExpressionBuilder<CrdtNodeTable> where,
    required _i1.LockMode lockMode,
    required _i1.Transaction transaction,
    _i1.LockBehavior lockBehavior = _i1.LockBehavior.wait,
  }) async {
    return session.db.lockRows<CrdtNode>(
      where: where(CrdtNode.t),
      lockMode: lockMode,
      lockBehavior: lockBehavior,
      transaction: transaction,
    );
  }
}
