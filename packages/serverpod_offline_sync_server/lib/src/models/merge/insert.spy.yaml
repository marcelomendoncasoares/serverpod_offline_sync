### A remote row insertion to merge.
class: CrdtMergeInsert
extends: CrdtMergeChange
fields:
  ### The domain row encoded by the application's serialization manager.
  ###
  ### Produced by `serializationManager.encodeWithType(domainRow)` and decoded
  ### by `serializationManager.decodeWithType(data)` on the receiving side.
  data: String
