name: drift_offline_first
description: A Drift plugin to build offline-first applications with CRDT support.
version: 0.0.1
repository: https://github.com/marcelomendoncasoares/drift_offline_first

environment:
  sdk: ^3.3.0

# TODO: Relax the package constraints to expand compatibility.
dependencies:
  crdt: ^5.1.3
  drift: ^2.28.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.6.0
  drift_dev: ^2.28.0
  lints: ^6.0.0
  test: ^1.26.2
