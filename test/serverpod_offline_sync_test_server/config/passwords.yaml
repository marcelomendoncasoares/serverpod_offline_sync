test:
  database: 'password'
  serviceSecret: 'super_SECRET_password'

# TODO: Make the `SqlQueryBuilder` use the `ValueEncoder` from the pool instead
# of the `ValueEncoder.instance`.

# TODO: Fix embedded postgres requiring the passwords file.
# packages/serverpod_shared/lib/src/config.dart:616-621
# ```dart
# var password = passwords[ServerpodPassword.databasePassword.configKey];
# if (password == null) {
#   throw PasswordMissingException(
#     ServerpodPassword.databasePassword.configKey,
#   );
# }
# ```
