class: Organization
table: organization
database: sync
fields:
  name: String
  people: List<Person>?, relation(name=org_person)
  city: City?, relation(name=city_org, optional, onDelete=Cascade, deferred)
