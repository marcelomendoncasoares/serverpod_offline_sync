class: RequiredSetNullChild
table: required_set_null_child
database: all
fields:
  id: UuidValue?, defaultPersist=random_v7
  ### Owner scope of this row. Maintained by the CRDT sync layer.
  scopeId: int?
  name: String
  parent: Person?, relation(field=parentId, onDelete=SetNull)
  parentId: UuidValue
