class: RestrictChild
table: restrict_child
database: all
fields:
  id: UuidValue?, defaultPersist=random_v7
  name: String
  parent: Person?, relation(field=parentId, onDelete=Restrict)
  parentId: UuidValue?
