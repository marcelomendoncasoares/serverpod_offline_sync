class: Town
table: town
database: all
fields:
  id: UuidValue?, defaultPersist=random_v7
  ### Owner scope of this row. Maintained by the CRDT sync layer.
  scopeId: int?
  name: String
  city: City?, relation(optional, onDelete=Cascade)
  mayor: Person?, relation(optional, onDelete=SetNull)
