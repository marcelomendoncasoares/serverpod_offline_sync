class: UniqueOverlapping
table: unique_overlapping
database: sync
fields:
  first: String
  second: String
  third: String
indexes:
  unique_overlapping_first_second:
    fields: spaceId, first, second
    unique: true
  unique_overlapping_second_third:
    fields: spaceId, second, third
    unique: true
