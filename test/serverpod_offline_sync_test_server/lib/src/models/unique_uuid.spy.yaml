class: UniqueUuid
table: unique_uuid
database: all
fields:
  id: UuidValue?, defaultPersist=random_v7
  ### Owner scope of this row. Maintained by the CRDT sync layer.
  scopeId: int?, relation(optional, parent=crdt_scopes, onDelete=Cascade)
  value: UuidValue, unique(per=scopeId)
