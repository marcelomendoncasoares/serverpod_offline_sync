class: UniqueUuid
table: unique_uuid
database: all
fields:
  id: UuidValue?, defaultPersist=random_v7
  ### Owner scope of this row. Maintained by the CRDT sync layer.
  scopeId: int?
  value: UuidValue
indexes:
  unique_uuid_scope_value_unique_idx:
    fields: scopeId, value
    unique: true
