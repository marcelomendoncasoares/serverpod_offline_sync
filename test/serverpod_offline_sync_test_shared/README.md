# serverpod_offline_sync_test_shared

This package contains the models shared by the Serverpod test client and server.
The code in `lib/src/generated` is generated by Serverpod from the model
definitions in `lib/src/models`.
